<%= form_for :artist, :url => url_for(:controller => 'artist', :action => 'create') do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :bio %>
  <%= f.text_field :bio %>
  <%= f.submit 'Create Artist' %>
<% end %>
